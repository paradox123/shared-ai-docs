# Blocked Agent Output

Promptfoo/Codex did not run because local credentials were not provisioned.
Fallback artifact validation may run deterministic checks, but the L2 agent
proof remains blocked and cannot be accepted as a pass.
