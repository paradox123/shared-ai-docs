# Mock Target

This directory is the isolated target root for the large-parent mock fixture.
`mock-target/output/count.txt` is created by the later local mock session runner and is not committed as fixture data.

