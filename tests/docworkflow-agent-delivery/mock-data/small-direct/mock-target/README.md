# Mock Target

This directory is the isolated target root for the small-direct mock fixture.
`mock-target/output/small-direct-result.json` is created by the later local mock session runner and is not committed as fixture data.

