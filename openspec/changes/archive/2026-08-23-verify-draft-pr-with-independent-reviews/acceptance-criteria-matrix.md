# Pre-implementation Evidence Matrix

| Issue 04 criterion | Public observation surface | Expected result | Planned direct proof |
| --- | --- | --- | --- |
| Active OpenSpec change | OpenSpec CLI | Goal, scope, write-set, tasks, and verification are apply-ready and strictly valid before runtime edits | `openspec status`, apply instructions, and `openspec validate verify-draft-pr-with-independent-reviews --strict` |
| Fresh independent read-only reviews on one head | Review-worker boundary plus signed HTTP workflow read-back | Three distinct invocations receive no peer verdicts, use read-only access, and name the same adapter-derived SHA | Worker adapter contract observes separate process inputs/access flags; system test reads three head-bound results |
| Schema-valid verdicts | Packaged JSON Schemas and workflow read-back | Each axis returns `pass`, `fail`, or permitted `not_applicable`, rationale, findings, and provenance; requirements cannot be N/A | Contract tests submit valid and deliberately invalid outputs and observe fail-closed state |
| Axis-specific review scope | Review assignment returned to controlled workers | Requirements covers spec/implementation/evidence, code covers standards/smells, architecture covers domain and module boundaries | Boundary contracts assert public assignment fields and routed skill contracts per axis |
| Terra/xhigh and traceable metadata | Review-worker invocation and workflow read-back | Every regular review uses Terra, `xhigh`, read-only, and retains model/reasoning/skills/hashes | Adapter contracts inspect launch contract; HTTP read-back asserts persisted metadata |
| Prescribed skill routing | Packaged routing contract and review-worker invocation | Requirements/code select separate `code-review` axes; architecture selects `codebase-design` and `domain-modeling` | Contract tests exercise all three roles through the worker port |
| Fail-closed aggregation and successful labels | Signed webhook POST, workflow-state GET, and controlled GitHub transport | One fail blocks all success labels; all applicable passes add `verified`/`awaiting-review` and remove `agent-running` | System cases observe independent results, current-head read, exact label effects, and no compensating verdict |
| Reviewers cannot mutate or deliver | Read-only worker CLI contract and controlled GitHub/source boundaries | Reviewer launches have no write profile; review path performs no branch write, repair, merge, deploy, or product-decision operation | Adapter contract asserts read-only sandbox; system effects expose review/label calls only |
| Primary-seam system behavior | Signed webhook POST and workflow-state GET with real SQLite/LangGraph | Blocked and successful review batches are durable, head-bound, externally observable, and restart-safe | Focused system tests use only HTTP read-back plus controlled worker/GitHub effects |
