## Why

Ticket 01 is implemented, locally accepted by the user and deployed to the Azure pilot. Extract its accepted GitHub intake contract from the wider GUI change so it can be archived without closing unfinished Tickets 02–16.

## What Changes

- Publish the already implemented GitHub intake requirement as the canonical Operator GUI baseline.
- Retain immutable redacted requirements, current human repository authorization, duplicate protection and browser readback without starting a run.
- Archive the local/browser/provider and Azure deployment evidence with the accepted slice.
- Keep background execution, file input, PRD decomposition, Handover and distributed overall acceptance in `add-agent-framework-operator-gui` and Tickets 02–16.

## Capabilities

### New Capabilities
- `agent-framework-operator-gui`: The accepted GitHub requirements intake baseline; the active parent change extends it with the remaining GUI workflow.

### Modified Capabilities
None.

## Impact

No runtime behavior changes in this extraction. The delivered API, PostgreSQL storage, browser client and Azure deployment assets are merged with their existing evidence. Local acceptance does not establish completion of Ticket 16.
