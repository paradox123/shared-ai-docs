# Implementation Evidence

| Acceptance criterion | Direct verification | Observed result | Status |
|---|---|---|---|
| Change defines target, scope, write set, and direct proof before implementation | `openspec validate add-bounded-review-repair-rounds --strict` before production/test edits | Proposal, design, bounded capability spec, tasks, and acceptance matrix validated strictly before implementation and now reside in the dated archive | proven |
| All failed-axis findings go only to the same writing implementer | `test_multi_axis_review_failure_is_repaired_once_and_new_head_verifies_through_http_seam` and repair-worker adapter contract | Requirements and code findings appeared together in round-one assignment; the original worker port/worktree had `workspace-write`; all reviewers remained separate/read-only | proven |
| Every repair commit gets deterministic verification and three fresh reviews on the new head | Successful signed-HTTP repair system test, `test_failed_deterministic_check_still_runs_all_reviews_and_enters_next_round`, and exact-head verifier contract | Repair heads differed from their predecessors, verifier commands were SHA-bound, and three new reviewer assignments ran even for the failed deterministic check before its next round | proven |
| At most three automatic rounds and no fourth | `test_review_failures_stop_after_exactly_three_repair_rounds_without_a_fourth_invocation` | Read-back contained rounds 1, 2, and 3 only; controlled worker had exactly three repair calls, verifier three, and reviewers 3 initial plus 9 repair calls | proven |
| Sol/`xhigh` only for allowed escalation/final round | Policy and repair-worker adapter contracts, `test_structured_escalation_uses_sol_inside_same_numbered_round`, and three-round system test | Regular attempts used Terra/`xhigh`; structured security escalation used Sol without incrementing the round; final attempt used Sol/`xhigh`/workspace-write and recorded `final_repair_round`; undefined reasons were rejected | proven |
| Exhausted missing requirements become `needs-info` with PR/history retained | `test_exhausted_human_handoff_survives_restart_without_duplicate_effects[missing-requirements]` | Three attempts, draft PR, open findings, and `needs-info` survived reconstruction with no repeated external effect | proven |
| Other exhausted conflicts become `ready-for-human` with PR/history retained | `test_exhausted_human_handoff_survives_restart_without_duplicate_effects[unresolvable-conflict]` | Three attempts, draft PR, open findings, and `ready-for-human` survived reconstruction with no repeated external effect | proven |
| Reversible details remain autonomous; semantic presentation remains a product decision | Repair assignment/result and CLI adapter contracts | Versioned decision policy and prompt permit small reversible details and require blocked/escalate output for warning, consent, domain-action, security, or other semantic decisions | proven |
| System behavior is durable through run, PR, and GitHub projection | Signed HTTP POST/GET system cases with real SQLite/LangGraph and controlled boundaries | HTTP read-back exposed verified and both terminal repair states; PR heads/bodies and label projections matched, and restart introduced no duplicate effects | proven |
