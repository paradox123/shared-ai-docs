# Design

This batch maps the final Nebenkosten OpenSpec source subset as relationship/evidence rows. It includes the remaining archived delivery artifacts plus the canonical OpenSpec spec for PDF payment instructions and prepayment recommendation.
