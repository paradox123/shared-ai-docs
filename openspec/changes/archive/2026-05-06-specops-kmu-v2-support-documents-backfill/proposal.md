# Proposal

## Why

The full historical backfill excludes generated shared-ai-docs archived OpenSpec artifacts. The remaining concrete non-excluded source gap is the KI-fuer-KMU v2 support document set: Application Flow, Slice Plan and S0 Freeze/Legacy Quarantine.

## What Changes

1. Create three `type: document` SpecOps entities for the remaining KI-fuer-KMU v2 support documents.
2. Update the source inventory, Control Spec and backlog to mark shared-ai-docs archived OpenSpec artifacts excluded and KI-fuer-KMU v2 docs complete at 6/6.
3. Keep runtime out of scope because this is metadata backfill only.

## Impact

No source project files are modified. This adds SpecOps document entities and control evidence only.
