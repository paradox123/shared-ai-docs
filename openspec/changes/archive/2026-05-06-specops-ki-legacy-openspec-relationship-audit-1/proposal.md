# Proposal

## Why

The KI legacy OpenSpec group is still only classified, and the full 35-file pool is too large for one manual import under the current scale rules. A first 21-file relationship batch keeps progress moving while avoiding an uncontrolled XL pass.

## What Changes

1. Add KI legacy OpenSpec batch 1 rows to the shared OpenSpec relationship audit.
2. Map the selected legacy v1 change groups to existing Mittelstand KI Startbahn entities.
3. Update Control Spec, source inventory and backlog evidence from classified-only toward partial KI legacy OpenSpec relationship coverage.

## Impact

No runtime code or historical source documents are modified. This is a metadata/reference update for SpecOps traceability.
