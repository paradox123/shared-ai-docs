# Proposal

## Why

KI legacy OpenSpec batch 1 mapped the first 21/35 legacy v1 files. The remaining 14 files form a cohesive final batch covering integration and artifact-pipeline evidence.

## What Changes

1. Add KI legacy OpenSpec batch 2 rows to the shared OpenSpec relationship audit.
2. Map the remaining legacy v1 OpenSpec groups to existing Mittelstand KI Startbahn entities.
3. Update Control Spec, source inventory and backlog evidence to close KI legacy OpenSpec coverage at 35/35.

## Impact

No runtime code or historical source documents are modified. This is a metadata/reference update for SpecOps traceability.
