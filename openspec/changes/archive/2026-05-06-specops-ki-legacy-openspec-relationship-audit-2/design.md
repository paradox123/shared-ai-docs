# Design

This batch completes the KI legacy OpenSpec relationship audit by mapping the two remaining v1 change groups:

1. `integration-01-04-e2e-gate`
2. `spec-04-artifact-pipeline-delivery`

Both groups are old v1 execution/evidence material. They are therefore represented as relationship/evidence rows against existing Mittelstand KI Startbahn entities, while the current v2 specs remain the primary implementation planning sources.
