## 1. OpenSpec and Contract Setup

- [x] 1.1 Strictly validate the active change, bounded write-set, and pre-implementation evidence matrix before runtime code changes.
- [x] 1.2 Add versioned assignment/result schemas, node policy, and skill-routing data with package loading support.

## 2. Vertical Red-Green Slices

- [x] 2.1 Add failing policy/routing contract tests, then implement exact deterministic/Luna/Terra/Sol, rights, escalation, feature/bug, triage, and slicing selection with fail-closed overrides and skill hashes.
- [x] 2.2 Add a failing signed-delivery HTTP test for bounded assignment/evidence planning before invocation, then implement issue context, assignment validation, pre-worker persistence, and read-back.
- [x] 2.3 Add a failing real-Git contract test for run-owned isolation, then implement the replaceable worktree port and `git worktree add` adapter.
- [x] 2.4 Add a failing fake-process contract test for non-interactive Codex invocation, then implement the replaceable worker port, CLI argument/stdin construction, JSONL capture, and structured result validation.
- [x] 2.5 Add a failing HTTP behavior test for a valid observable Red-Green result across restart, then extend the graph, durable store, and workflow read model through successful implementation.
- [x] 2.6 Add failing HTTP behavior tests for invalid/failed result containment and duplicate delivery, then persist failure without another worktree, invocation, checkout change, sibling change, or pull-request write path.

## 3. Refactoring, Documentation, and Evidence

- [x] 3.1 Refactor the touched code, tests, schemas, and specs for DRY, SOLID, and KISS issues while preserving behavior.
- [x] 3.2 Update pilot runtime/configuration documentation and the active capability specification.
- [x] 3.3 Run the full behavior suite, direct HTTP/Git/CLI contract scenarios, lint/lock checks, `git diff --check`, and strict OpenSpec validation.
- [x] 3.4 Record criterion-level implementation evidence and mark Issue 02 criteria complete only where directly proven.
