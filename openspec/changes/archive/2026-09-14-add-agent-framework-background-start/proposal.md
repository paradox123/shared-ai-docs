## Why

The user accepted Ticket 02 after the real background analysis, failure and recovery proofs. Extract its completed contract from the wider GUI change so it can be archived and merged without closing unfinished Tickets 03–16.

## What Changes

- Publish the delivered new/saved GitHub submission start as a canonical Operator GUI requirement.
- Retain atomic run/disposition creation, current authorization, immutable mandate and duplicate protection.
- Preserve independent real analysis, correlated session/history/artifacts, truthful GUI results and uncertain-response reconciliation.
- Archive the acceptance and review evidence with this completed slice; retain the remaining GUI workflow in `add-agent-framework-operator-gui`.

## Capabilities

### New Capabilities
None.

### Modified Capabilities
- `agent-framework-operator-gui`: Extend the accepted intake baseline with the independently executed first requirements-analysis step.

## Impact

No new runtime behavior in this extraction. The already implemented API, worker, PostgreSQL dispatch, Codex adapter and GUI are delivered with their tests. Full implementation/review/repair remains Ticket 09; deployment and separate-machine overall acceptance remain outside this accepted local slice.
